# ---------------------------------------------------------------------------
# VEYRA web uygulamas? ? build ortam de?i?kenleri (?RNEK)
# ---------------------------------------------------------------------------
# Ger?ek de?erler i?in bu dosyay? KOPYALAYIN:
#   cp .env.example .env.production.local        (yerel geli?tirme, Git'e girmez)
# veya CI'da ayn? adlarla secret/env tan?mlay?n (Vite, VITE_* s?re? env'ini okur).
#
# NOT: Vite ?nceli?i ? s?re? ortam de?i?keni > .env.production.local > .env.production
# Bu y?zden ?rnek dosya bo? b?rak?lm??t?r; bo? de?erler ger?ek bir env'i EZMEZ.

# Clerk publishable key (pk_test_... / pk_live_...).
# Tan?ml? de?ilse uygulama yine a??l?r; katalog + player ?al???r,
# ancak oturum a?ma (Sign in / Profile / Admin) pasif kal?r.
VITE_CLERK_PUBLISHABLE_KEY=

# Backend taban adresi.
# - Taray?c?/Replit derlemesinde bo? b?rak?n ? g?reli /api/... kullan?l?r.
# - ANDROID bundle modunda g?reli ?a?r?lar WebView'in kendi origin'ine gider ve
#   ba?ar?s?z olur; bu y?zden APK derlerken MUTLAK adres verin, ?r.:
#   VITE_API_BASE_URL=https://veyra-ornek.replit.app
VITE_API_BASE_URL=
