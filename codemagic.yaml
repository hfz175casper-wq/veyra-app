workflows:
  android-release:
    name: Veyra Android Release
    max_build_duration: 30
    environment:
      node: 22
    scripts:
      - name: Install dependencies
        script: |
          YARN_IGNORE_WORKSPACE_ROOT_CHECK=1 yarn install
      - name: Build web app
        script: |
          YARN_IGNORE_WORKSPACE_ROOT_CHECK=1 yarn build
      - name: Sync Capacitor and Build APK
        script: |
          npx cap sync android
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease
    artifacts:
      - android/app/build/outputs/apk/release/*.apk
